<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:Pokedex.ViewModels" x:Class="Pokedex.Views.PhotoPage" Title="Camera" BackgroundColor="#121212">
    <ContentPage.Content>
        <StackLayout x:DataType="vm:PhotoViewModel">
            <Button Command="{Binding TakePhotoCommand}" Text="Prendre Photo" />
            <Button Command="{Binding NavigateCommand}" Text="Navigate" />
            <Label Text="PhotoPath" />
            <Label Text="{Binding PhotoPath}" />
            <Image Source="{Binding PhotoPath}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>